services:
  kato:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: local/kato:latest
    platform: linux/amd64
    container_name: kato
    restart: unless-stopped
    tty: true
    volumes:
      - ../../data:/app/data
      - ../../output:/app/output
      - ./data:/app/mcp/xiaohongshu/data
      - ./images:/app/mcp/xiaohongshu/images
    environment:
      - PORT=4173
      - XHS_SERVICE_PORT=18060
      - XHS_BROWSER_BIN=/usr/local/bin/kato-chromium
      - XHS_CDP_HOST=127.0.0.1
      - XHS_CDP_PORT=9224
      - XHS_INTERNAL_CDP_PORT=9224
      - XHS_DISPLAY=:99
      - XHS_VNC_ENABLED=1
      - XHS_VNC_PORT=5900
      - XHS_NOVNC_PORT=6080
      - XHS_PROFILE_DIR=/app/mcp/xiaohongshu/data/profile
      - COOKIES_PATH=/app/mcp/xiaohongshu/data/cookies.json
      - LANG=zh_CN.UTF-8
      - LC_ALL=zh_CN.UTF-8
      - LANGUAGE=zh_CN:zh
    ports:
      - "4173:4173"
      - "18060:18060"
